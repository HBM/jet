- var comp = contents['comparison.json'].metadata;
table.table
  thead
    tr
      th
      each title in comp.names
        th= title

  tbody
    each feature in comp.features
      tr
        td= feature.name
        each val in feature.value
          td= val        


